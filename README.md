# 🧩预设工坊 · 测试版

当前实验版本：**v2.98.0-test.38**，已同步正式版至 v2.97.36（`a52e18b`）。安装测试版时请停用正式版和旧脚本，避免同时运行。

## test.38：桌面旧布局类兼容

- 修正桌面根节点仍带历史 `pmm-mobile-layout-enabled` 类名时，顶部和侧栏样式被误排除的问题。
- 约 750px 宽的桌面单面板会把顶部右侧工具稳定排为两行；不会增加横向滚动条。
- 单栏侧边工具栏以实际主面板为锚点，首次打开不再留在外层容器最右侧。

## test.37：桌面顶部换行与首次打开侧栏定位

- 删除 test.35/test.36 的桌面横向滚动条实现；窗口收窄时，顶部右侧工具按钮在可用空间内自动排为两行。
- 不再依赖 Tauri 首次打开时不稳定的鼠标／悬停媒体能力，只要窗口处于桌面宽度就加载桌面样式。
- 恢复保存的窗口尺寸时，单栏侧边工具条按实际主面板宽度定位，首次打开也不会跑到屏幕最右边。

## test.36：桌面滚动条与单栏箭头修复

- 覆盖标题卡片隐藏滚动条的旧样式，溢出时显示可拖动的细滚动条，并恢复里框鼠标交互。
- 修正恢复桌面窗口尺寸后单栏面板与外层宽度不一致，侧边展开箭头紧贴面板。
- 补充隐藏滚动条样式冲突、实际鼠标拖动和不同保存宽度下箭头位置的浏览器回归；手机布局保持不变。

## test.35：桌面顶部独立横向滚动

- 仅在精确指针、支持悬停且未启用手机布局时生效，手机布局和滚动行为保持不变。
- 桌面标题里框与外层顶部工具栏分别按需横向滚动，图标不缩小、不换行；取消旧的窄容器换行规则。
- 两个区域使用细滚动条并隔离横向滚动边界，不改下方正文和快照列表的纵向滚动。

## test.34：预设快照条目只读正文

- 点击预设条目名称展开／收起当前预设正文，供电开关独立操作。
- 正文复用打开编辑器时已读取的 prompts，仅保存在编辑会话内；首次展开才创建正文 DOM，组内条目继续按需渲染。
- 沿用世界书的只读说明、换行与限高滚动样式，正文不进入快照存储，保存和应用格式保持不变。

## test.33：恢复全局应用文案与浅色标题外框

- 预设快照按钮恢复完整「全局应用」文案，短名称与按钮同一行，长名称完整显示并将按钮整体换到下一行。
- 预设选择、搜索与铅笔所在标题卡片按实际面板明暗调整外框；日间与跟随主题的浅色面板使用清晰的文字色边框，深色保持原边框。

## test.32：统一快照交互与自动恢复边界

- 预设分组标题整行可展开／收起，供电开关保持独立；去掉工坊世界书上下栏相机，保留酒馆原生入口。
- 手机角色／全局世界书弹窗底部对齐，编辑页采用大面板；分组开关隔离宿主背景框与阴影。
- 世界书分组先选择书，点击保存后再弹窗命名，返回编辑保留选择。
- 当前预设没有应用或绑定时，聊天切换不再兜底恢复默认；自动切换状态一致时不重复写入或提示。

## test.31：单行快照卡片与未分组条目顺序

- 预设快照卡片的名称、角色锁、聊天锁、应用与更多按钮保持同一行；长名称及摘要使用省略号，完整名称保留在提示中。
- 新建预设快照时，未分组条目按原预设顺序直接显示在分组前后，不再归入底部的合成分组。
- 分组仍按需展开，独立条目开关与分组供电继续使用隔离草稿，保存格式兼容现有快照。

## test.30：快照保存命名与手机界面调整

- 手机预设快照管理弹窗恢复底部对齐，并继续跟随可视区域与安全区。
- 预设、角色世界书和全局世界书的快照编辑页移除名称栏，点击保存后统一弹窗命名；返回编辑保留草稿，确认命名后才写入快照。
- 预设开关缩小为 30 × 18px 滑轨，保留 44 × 36px 点击区域，隔离宿主按钮背景、阴影与伪元素；整理分组行与说明间距。

## test.29：独立轻量预设开关快照编辑器

- 工坊内部与工坊浮动栏不再放置相机；预设快照入口只保留在酒馆原生预设页，角色／全局世界书入口保持原样。
- 点击「新建开关快照」后关闭管理小弹窗，打开拥有独立 Overlay 与生命周期的大号轻量编辑器；保存或取消都会销毁编辑器并返回原管理弹窗。
- 编辑器只读取当前预设 prompts 与柏宝箱真实分组数据，初始仅创建分组卡片，展开分组时才按需生成条目 DOM。
- 条目开关与柏宝箱分组供电分别保存在隔离 draft 中，互不联动；取消不写入任何真实状态，保存只追加兼容旧格式的快照且不会自动应用。
- 手机端适配 VisualViewport、安全区和 16px 名称输入框；打开编辑器不会主动聚焦，因此不会无故弹出键盘。

## test.28：桌面端面板四角拖动自由缩放

- 面板四个角均作为对角缩放热区（左上/右下为 `nwse` 光标，右上/左下为 `nesw` 光标）。
- 拖动任意一个角均以面板当前中心位置为基准整体对称扩缩，不固定宽高比例。
- 动态模式边界保护：双栏模式（缝合/分支/收藏）最小可用宽度动态限制为 980px，单栏模式为 560px，最小高度 420px，防止双栏挤压。
- 尺寸持久化与双击重置：缩放尺寸自动持久化至 localStorage；双击任意一个角恢复桌面端默认尺寸并清除本地记录。
- 严格环境与生命周期隔离：仅桌面端启用（结合 `pointer: fine` 与 `hover: hover` 排除触屏平板），仅在拖动期间临时监听 pointermove，结束后立即清理；不遮挡面板拖拽、关闭按钮与滚动条。

## test.27：自动切回绑定聊天快照通知

- 当从其他聊天切回已绑定世界书快照的聊天且世界书开关实际需要恢复/重新应用时，自动提示：`已切回绑定聊天快照：{快照名称}`。
- 开关状态无需变化、手动点击绑定/应用、打开关闭面板、同聊天内重复事件、页面刷新或初始化加载均不触发通知；切回失败保留原错误警告。
- 同一次真实聊天切换严格按 chat 身份去重，不依赖固定延时掩盖重复。

## test.26：移动端性能收尾优化

- 快照编辑器中折叠状态的世界书不再预渲染全部条目 DOM；展开时才按需生成，并立即应用当前搜索词，不会短暂闪现全部条目。
- 条目正文（只读预览）改为首次展开时才填入 DOM，关闭后再展开复用已有内容，不再预先插入所有正文。
- 批量管理世界书时，勾选或取消单个项目只更新该行的视觉状态和底栏计数，不再重建整个列表 DOM。
- 为快照条目行、批量管理行和快照卡片添加 CSS `content-visibility: auto`，浏览器可跳过离屏元素的渲染。
- 不删除任何毛玻璃、动画或 UI 效果；不改变 UI 外观、按钮位置、功能逻辑、用户操作流程或快照数据格式。

## test.25：按相机入口精简快照分类

- 预设页面的小相机只打开预设快照，不再显示或恢复角色世界书、全局世界书页面。
- 世界书页面的小相机只显示「角色世界书／全局世界书」两个 Tab，不再出现「预设」。
- 世界书入口独立记住上次停留的角色／全局分类；旧记录若停在预设会被忽略，不影响现有快照数据。
- 两个入口仅重新组织导航，预设、角色世界书、全局世界书的默认方案、快照和绑定数据保持不变。

## test.24：隔离酒馆勾选框美化

- 快照条目的可见开关恢复到原先约 32 × 19px，外层保留约 44 × 36px 的透明点击区域，外观紧凑且手机仍容易点击。
- 原生 checkbox 只负责状态并完全透明，独立滑轨负责绘制圆点与颜色；酒馆美化即使强制添加勾号，也不会再出现在快照开关上。
- 角色世界书和全局世界书统一生效，不影响条目正文浏览、快照保存或应用逻辑。

## test.23：快照条目改用普通滑动开关

- 角色世界书和全局世界书的快照编辑页改用无勾号的普通滑动开关，视觉与酒馆原生世界书一致。
- 开关放大至约 44 × 26px，手机端更容易点击；开启时圆点滑向右侧并跟随当前主题色，关闭时回到左侧灰色状态。
- 样式只作用于快照条目，不影响世界书分组选择，也不改变开关数据、保存或应用逻辑。

## test.22：新建快照不再隐式应用并记住分类

- 新建角色世界书快照现在只保存开关方案，不再自动写入当前世界书，也不会立即显示为「当前」。
- 手动点击「应用」仍会应用方案；开启当前「聊天锁」仍会绑定并立即应用，原有自动恢复逻辑保持不变。
- 世界书页的小相机也会恢复上次停留的「预设／角色世界书／全局世界书」，不再强制跳回全局世界书。
- 调整开关页的底部说明和保存提示同步改为真实行为，避免误以为保存等于应用。

## test.21：展开角色世界书时保持位置

- 修复点击底部「角色绑定世界书」后列表重新渲染并跳回非角色列表顶部的问题。
- 展开后将角色区标题定位到滚动区域顶部，第一批角色绑定世界书会直接出现在下方。
- 勾选、全选和删除过程保留当前滚动位置，不再因状态重绘反复跳回列表顶部。

## test.20：优先清理非角色世界书

- 批量管理优先展示非角色绑定世界书；角色绑定世界书移到底部并默认收起，需要时点击展开。
- 顶部全选只作用于当前实际展示的项目，不会暗中选中收起的角色世界书。
- 收起角色绑定区时自动取消其中的勾选，避免隐藏选择后误删。

## test.19：批量世界书按绑定分组

- 打开批量管理时不再自动聚焦搜索框；只有手动点击搜索框才会弹出手机键盘。
- 世界书按「角色绑定世界书／非角色绑定世界书」分段显示；角色绑定项同时提示绑定角色。
- 搜索继续过滤全部世界书，没有匹配项的分段会自动隐藏，不改变批量删除和快照清理逻辑。

## test.18：原生世界书快捷入口与批量删除

- 在酒馆原生「世界／知识书」标题栏增加批量管理和快照相机；相机直接打开全局世界书快照。
- 批量管理保持简单，只提供搜索、多选、全选与批量删除，不加入挂载、导出或建组等额外操作。
- 批量删除复用酒馆助手的世界书删除逻辑，并统一清理世界书分组、分组默认方案和命名快照中的失效书名。
- 从酒馆原生垃圾桶删除世界书时同样自动清理；保留分组内其他书和快照数据，分组清空时只自动关闭、不删除空分组。

## test.17：明确预设应用并收紧名称框

- 仅将「预设」快照的普通「应用」按钮改为「全局应用」；已经生效时仍显示「当前」，角色和全局世界书用词不变。
- 角色／全局世界书的快照编辑页缩短 Tab 与「快照名称」之间的留白。
- 快照名称框收紧至约 38px，并让右侧铅笔相对输入框精确垂直居中；保留 16px 的 iOS 安全字号。

## test.16：快照状态与只读正文

- 世界书分组编辑区取消突兀的实体背景，并缩短 Tab 下方留白；分组名称、说明与搜索框融入弹窗底色。
- 每本世界书的条目搜索框收紧到约 34px，同时保留 16px 的 iOS 安全字号。
- 角色世界书快照应用后显示不可重复点击的「当前」状态；聊天锁即时应用时同步更新状态，恢复默认后重新显示「应用」。
- 角色和全局世界书的快照编辑页支持点击条目名称展开当前世界书正文；正文只读，展开不会改变右侧开关，快照仍只保存开关状态。

## test.15：同步正式版 v2.97.22–v2.97.36

- 移植酒馆主预设栏的批量管理与快照相机入口，以及快照应用后的原生开关同步和重绘。
- 快照制作使用冻结基线与隔离事务，条目／分组操作不再污染主预设，首次应用也会立即同步。
- 保留从原生主预设页进入并返回原页的行为，同时收紧点击事件边界，避免 TauriTavern 全局按钮被拦截。
- 修复批量管理预设的搜索筛选，并合入正式版截至 v2.97.36 的其他有效修复；已被正式版撤销的实现不会带入。
- 测试版原有的角色世界书、全局世界书、世界书分组与快照功能全部保留。

## test.14：收紧分组首页顶部留白

- 单独缩短「全局世界书」Tab 与「＋ 新建分组」之间的纵向间距，减少手机端空间占用。
- 其他 Tab、分组卡片内部及编辑页面的间距保持不变。

## test.13：分组首页收紧与快照清理

- 删除全局世界书首页多余的单项「世界书分组」导航行，内容直接从新建分组开始。
- 方案下拉按当前选中文字自适应；「默认」保持短小，长名称限制宽度并截断，不受「＋ 新建分组快照」长度影响。
- 分组快照默认方案增加小垃圾桶；仅停用分组时可确认删除，删除不写入世界书，下一次启用或新建快照会自动重建。
- 删除分组时同步清理该分组的默认方案和命名快照，保留世界书文件，避免残留孤儿数据。
- 分组名称和搜索框收紧到约 34px，同时保留 16px 的 iOS 安全字号。

## test.12：分组内联管理快照

- 取消独立的「分组快照」顶部入口，全局世界书以「世界书分组」为唯一首页。
- 每个分组的方案下拉最后增加「＋ 新建分组快照」，直接带入当前分组；保存或取消都回到分组页，不更改原已选方案。
- 分组菜单改为「编辑分组／管理分组快照／删除分组」，管理页继续复用原有新建、编辑开关、改名与删除逻辑。
- 分组名称框缩小并增加居中铅笔；新增「搜索世界书」，只过滤当前可选书单。
- 保留现有分组快照数据结构、默认方案、当前方案与开关行为，旧独立首页记忆会安全回到分组页。

## test.11：分组编辑操作栏常驻

- 「分组名称」固定在编辑区顶部，只让中间的世界书列表滚动，长列表中不再丢失名称输入框。
- 底部「取消／保存」跟随手机真实可视区域，键盘弹出前后都常驻可见，不再需要滚到页底。
- 仅当分组名称已填且至少选择一本世界书时启用「保存」，避免保存后再被迫返回补名称。
- 补充窄屏、缩小可视区域、长列表和保存条件回归测试；已有分组、快照和开关数据逻辑不变。

## test.10：世界书外观严格跟随预设

- 世界书快照直接复用预设的遮罩、面板、标题栏与关闭按钮组件，统一背景明暗、边框、阴影和四角圆度。
- 角色世界书的聊天锁、应用和省略号解除世界书通用最小高度影响，恢复预设的扁平紧凑尺寸。
- 保留 460px 默认工作高度、编辑页高度以及 test.9 的导航、逐书搜索与数据行为。

## test.9：分组返回与角色快照统一

- 修复「分组快照首页 → 选择分组 → 返回」错误进入旧分组详情；选择页现在精确恢复进入前页面，不修改任何快照数据。
- 调整开关页给名称输入框增加可聚焦的铅笔提示；搜索移入每本展开的世界书，只过滤该书条目。
- 角色世界书列表直接复用预设的默认区、新建区、快照卡片、操作区和聊天锁组件；聊天锁使用同款图标与黄色绑定状态。
- 角色默认区增加与预设同款垃圾桶；确认后先恢复默认，再清理当前角色的默认方案与快照，不影响预设和全局世界书。
- 新增导航、逐书搜索、名称聚焦、共享组件及角色重置回归测试；仍需 iOS／TauriTavern 真机确认。

## test.8：统一快照样式与返回位置

- 角色／全局世界书跟随预设的简洁标题、描边按钮与默认区样式；保留分组方案下拉选择。
- 预设角色锁、聊天锁恢复卡片原位，沿用正式版绿色／黄色绑定状态。
- 世界书省略号使用竖向图标菜单，按可用空间上下展开，不挤高卡片或覆盖开关。
- 关闭后记住分类、全局子页及所选分组；预设相机和世界书相机均可恢复，明确切换分类优先。
- 浏览器夹具覆盖菜单、分类恢复、窄屏和主题；真机效果仍需 iOS／TauriTavern 确认。

## test.7：原生世界书同步

- 小菜单移到卡片主操作下方，不遮挡分组开关；移除「组选用」，统一在分组卡片的方案下拉框选择。
- 快照修改条目后，直接通知当前选中的原生世界书编辑器刷新，不再依赖工坊世界书面板打开；不切换到其他书。工坊自己的数据刷新保留，并避免重复刷新原生面板。
- 全局绑定接口完成后发出原生 change／Select2 视图通知，挂载标签随分组开启、关闭和回滚更新，无定时轮询。
- 浏览器夹具补测原生选择框监听、工坊关闭时条目重绘、全局标签增减及菜单不遮挡开关；仍需 iOS／TauriTavern 真机确认。

## test.6：快照管理入口与编辑

- 「⋯」改为紧凑浮动菜单，点击外部收起；新建快照固定排在默认及已有快照上方。
- 成功提示 3 秒自动消失，也可点 ×；错误提示可手动关闭，切换页面清理旧提示。
- 分组快照首页列出已有快照的分组和数量，点击可回到管理页；「选择分组」仍可给未有快照的分组新建。
- 更多菜单新增「编辑开关」，从保存的快照载入草稿，取消不写入，保存保留 ID、默认及绑定。若快照被开启分组选用，保存前确认是否同步应用；取消同步则仅保存方案。共享书冲突仍需确认。

## test.5：原位方案下拉与书目折叠

- 分组卡片直接下拉选择「默认／该组快照」，不再打开独立选择页；没有快照时只有默认。
- 开启分组的编辑／删除不再静默禁用：点击后确认先关闭再操作。编辑后需手动重新开启；取消确认不改变分组。删除仅移除分组记录，不删除世界书文件，仍保留手动及其他分组挂载。
- 快照编辑按世界书分块，显示书名、条数及展开箭头。多本默认收起，一本默认展开；搜索会展开匹配项，清空搜索恢复之前的折叠状态。折叠不丢开关草稿。
- 补充手机／桌面下拉、开启分组编辑删除、两书独立开关读写及搜索折叠回归。

## test.4：角色聊天与分组方案

- 角色页仅在角色聊天内可用，不再搜索全部角色；一份快照记录当前角色绑定的全部世界书开关。首次新建先保存默认，后续新建不覆盖默认。
- 聊天锁绑定当前聊天并立即应用；同一角色的其他聊天不沿用此锁。返回主页／切换聊天恢复进入前状态，默认基准与临时恢复记录分开。解除锁保留当前开关。群聊暂不支持。
- 全局页改为「世界书分组／分组快照」；先选择分组，再创建整组开关方案。保存分组快照不会挂载或修改原书，在分组卡片「方案」里选择默认或新快照，开启时挂载并应用；已开启时切换方案立即生效。
- 共享世界书开关冲突会确认是否使用当前方案，取消不覆盖。关闭分组保留手动挂载及其他开启分组需要的书；修改成员会重置为默认，旧成员不匹配的快照不会强行应用。
- 分组卡片采用小开关；编辑／删除收进「⋯」。日间、夜间、魔法棒颜色和 460px 默认高度保留。
- 旧版单书快照及绑定数据保留，不自动合并为新方案；新界面使用整组／当前角色方案。为角色创建新方案后，该角色不再沿用旧的单书角色自动绑定，以新聊天锁为准。此轮不改正式版。

以下为旧版本实现记录，当前行为以上面的 test.4 为准。

test.3：三类快照默认面板统一加高至 460px（受当前可视区域限制）；修复仅从浮动栏打开时缺失主题文字色的问题。世界书选择列表支持角色姓名／世界书名称搜索，输入时保持焦点和中文输入状态。全局页将「世界书分组」放在最前，并默认打开分组页。

test.2：相机首次打开直接显示紧凑快照页，点击「选择世界书」再浏览列表；长列表在卡片内部滚动。三类快照采用柔和圆角、浮起按钮和简洁线条图标，颜色跟随日间、夜间及魔法棒主题；输入编辑时使用更高的卡片，并继续随键盘可视区域调整。

## 世界书快照第一版

- 世界书标题栏的相机打开快照页；点击「选择世界书」显示上下分区：角色绑定世界书、已挂全局且未绑定角色的世界书。
- 快照面板可以切换「预设」「角色世界书」「全局世界书」。预设原有的绑定操作收进「⋯」。
- 选择一本世界书后新建快照，只编辑条目开关。草稿和保存都不改原书；手动点击「应用」或开启「聊天锁」后才应用，取消不写入。快照不复制正文、关键词或排序；新增条目保留当前开关，删除的条目不重建。
- 创建快照默认不绑定角色。在对应角色聊天中，用「⋯ → 绑定当前角色并应用」立即应用，以后进入角色自动应用。返回主页或切换角色时先恢复进入前状态，再应用新角色的绑定方案。
- 多个角色可以共用一本书，但同一个角色对同一本书只绑定一份快照。第一版不添加聊天专属世界书快照或群聊绑定。
- 「全局世界书 → 世界书分组」可以选择尚未挂全局的未绑定角色世界书。开启会统一挂载；关闭保留原本手动挂着和其他开启分组需要的书。先关闭分组才能改成员或删除。
- 自动切换监听聊天变更事件，不轮询世界书正文。世界书未保存的普通编辑会阻止覆盖；编辑快照时切换角色会保留草稿并阻止保存到错误上下文，取消草稿后继续切换。

快照和恢复记录保存在当前浏览器的本地存储，应用后的世界书与全局挂载会写入酒馆。多设备或多标签页同时调整同一本书不属于第一版的隔离范围。开启分组期间若在酒馆原生页面反复手动调整相同挂载，无法可靠识别一次「重新手动接管」；建议先关闭分组再手动管理。

本仓库是正常浏览器版，不包含 Gecko 后台 iframe 实验补丁。Firefox、雨见等 Gecko／GeckoView 手机浏览器请改用 [Gecko 兼容测试版](https://github.com/yui-ovo/ST-Preset-Workshop-Gecko)，请勿同时安装两个版本。

## 依赖

本扩展继续使用酒馆助手提供的脚本运行环境，因此需要先安装并启用：

- [酒馆助手（JS-Slash-Runner）](https://github.com/N0VI028/JS-Slash-Runner)

安装扩展版前，请在酒馆助手中停用旧的 `🧩预设工坊｜双端适配v2.53` 脚本，避免重复运行。

## 安装

1. 打开 SillyTavern 的“扩展”。
2. 选择“安装扩展”。
3. 粘贴本仓库的 Git URL：`https://github.com/yui-ovo/yushe-gongfang-ceshi.git`。
4. 安装完成后刷新 SillyTavern。

## 当前版本

- 扩展版本：`2.98.0-test.25`
- 紧急修复 TauriTavern／SillyTavern 全站按钮失效：主预设返回页事件边界不再使用会从 `<html class="pmm-mobile-toolbar-ready">` 命中任意点击的模糊 `pmm-*` 选择器，只拦截小相机、快照／批量弹层、悬浮栏和工坊主面板五个明确区域；扩展开关、管理与删除按钮恢复正常。
- 修复批量管理预设的搜索框在部分手机 WebView／主题下输入后列表不筛选的问题：匹配结果改用专用隐藏样式，不再依赖可能被列表 `display:flex` 覆盖的 `hidden`；同时兼容搜索清除与中文输入法完成事件，并在无匹配时显示明确提示。
- 重新实现酒馆主预设页的返回保留：沿用已验证助手脚本的事件边界，在酒馆 `body` 冒泡阶段拦截工坊相关的 `mousedown`、`pointerdown`、`touchstart` 与 `click`；从主预设小相机进入、制作或保存快照、退出工坊后，原主预设页继续保留，不再回到聊天主页。
- 修复快照模式切换条目或柏宝箱分组后，保存退出偶尔没有恢复进入前状态的问题：录制期间的全部开关现在只修改隔离画面，不再提前写入命名预设或酒馆原生分组存储；进入前即使已有未保存修改，也会恢复该草稿并保留原有保存状态。
- 快照录制改为真正的隔离事务：每次进入时冻结完整主预设基线，保存或取消后直接整体恢复，不再读取退出瞬间可能滞后的 Vue 草稿，避免第二次制作快照时偶发残留快照开关。
- 修复从酒馆主预设栏相机第一次应用快照时，隐藏工坊旧草稿可能令应用被误判为零变化、主预设开关不刷新的问题；原生入口现在以酒馆当前预设为准，并始终完成一次幂等同步与重绘。
- 修复从酒馆主预设栏相机进入快照后，点击应用、绑定等弹层按钮会被酒馆误判为点击菜单外部、进而关闭主预设页面的问题。
- 快照模式现在只保留条目开关与柏宝箱分组开关；条目编辑、收藏、复制、删除、框选分组及分组改名／解散按钮均会隐藏，退出快照模式后恢复正常。
- 修复比对详情中“仅上侧／仅下侧预设存在”的条目位于折叠分组时无法跳转的问题；现在会先自动展开对应分组，再滚动并高亮准确条目，同名条目也按出现顺序定位。
- 修复普通预设条目多选拖动时因勾选先后与逐条落位造成的内部乱序；现在无论向上或向下、落在目标上方或下方，都会保持条目在当前列表中的原始相对顺序。
- 修复 iOS 键盘弹出时“保存快照”名称输入框将底部弹层推离可视区域的问题；安卓、Gecko 与电脑端继续沿用原定位路径。
- 在酒馆主页手动应用的快照会作为“主页快照”记忆；角色或聊天绑定会临时覆盖并在绑定当下立即生效，回到 `No chat selected` 或进入无绑定聊天时恢复主页快照，主页快照失效后才回退预设默认。
- 预设正文的轻量全屏编辑器改为随工坊独立加载；无需先打开世界书，首次点击预设条目的放大按钮即可进入留边正文编辑框，世界书仍保持按需加载。
- 按正常点击逻辑处理预设、分支、收藏分类和普通分组的外层改名铅笔，不再在安卓等触屏设备的按下阶段提前触发改名。
- 主预设页新增“开关快照”：可保存、更新、恢复和管理预设默认快照、角色／聊天绑定快照与命名快照；支持普通条目和柏宝箱分组总开关，快照捕获时直接显示分组原生开关。
- 快照入口只在主预设页显示；手机默认会让搜索、铅笔和相机完整露出，手动调整名称框长度后恢复横向滑动，极窄屏会额外收短标题外框以保留右侧关闭按钮。
- 快照弹窗在手机浏览器中按真实可视区域定位，并会在地址栏伸缩、滚动及横竖屏变化时重新校正；快照模式中的原生保存按钮保持暗色，触摸展开不会被延迟点击再次关闭。
- 电脑主面板与双栏分屏采用更紧凑的默认宽度和 52:48 分配，标题栏及快照保存／退出按钮同步适配；分支、收藏、正则、世界书等其他页继续隐藏相机并保证铅笔可见。
- 手机夜间模式的顶部工具按钮在未选中时不再显示一格一格的底框，与日间模式保持一致；当前启用、保存高亮和按压状态仍有反馈，取消多选后也不会残留左侧竖线。
- 手机端原生预设与上方世界书统一为固定标题外框：默认会优先缩短内部名称，确保搜索和小铅笔可见；手动调节“预设名称框长度”时只改变内部名称，下方世界书不受影响。
- 标题右侧工具会按当前屏幕剩余宽度平均分布；不常用的导入、导出仍保留在可横划到的右侧区域。
- 工坊不再把全局顶部通知强制缩短为 0.3 秒，通知开启时会遵循酒馆自身的显示时长，也不会影响其他插件的通知速度。
- 布局调节页底部新增“顶部通知”开关；关闭后会记住选择并静默预设工坊与世界书的顶部小通知，不影响酒馆和其他插件。
- 精简日常通知：保存、改名、切换、单条收藏／复制等已有可见结果的操作不再重复弹窗；错误、风险提醒、撤销、批量操作和跨模块同步结果继续保留。
- 新增世界书入口：支持世界书条目之间拖动缝合、世界书条目拖入预设自动转换，并支持同一本世界书内的视觉排序。
- 世界书支持角色绑定搜索、查找替换、展开条目复制／删除、蓝绿灯切换，以及保存前草稿与保存按钮高亮提示。
- SillyTavern 1.18 及以上继续使用原生世界书枚举接口；1.14 等旧版缺少该接口时，会按酒馆原生方式读取世界书名称。
- 世界书编辑仅在打开“世界书”时启用；只使用预设缝合时不会额外开启这套模块。
- 迁移基准：`🧩预设工坊｜双端适配v2.53`
- 第一阶段保持原脚本行为，不在迁移过程中重写业务逻辑。
- iOS、iPadOS 与 TauriTavern（WKWebView）会自动启用稳定滚动模式；安卓继续使用原有离屏渲染优化。
- 魔法棒跟随主题时会忽略透明假黑，优先采用酒馆主题色，并同时校验面板、条目卡片与文字对比度。
- 打开缝合后，下方预设的分组内、分组外普通条目开关都会立即同步；柏宝箱分组总开关保持原行为。
- 进入多选并勾选条目后，展开任意条目点击正文旁的 `S`，可把所有已选普通条目批量转换为同名变量；转换后的正文会立即同步到当前分组视图，`G` 保持原行为。
- 多选变量条目后可批量重命名变量，并按剩余同名正文安全保留、新增或同步 `G` 与同名空变量项；斜线 `S` 可去除变量格式并选择是否清理失效项。
- 预设条目正文支持只显示正文的留边全屏编辑，跟随当前主题，并提供本次输入撤销、取消与保存。
- 单独更新本扩展时会保留酒馆原生成功提示并自动刷新；批量更新时不会抢先打断其他扩展。
- 多选模式下，每个分组标题提供双勾按钮，可一键选择或取消本组全部条目；在主预设分组内把连续条目组成工坊分组时，子分组保持在原位置。
- TauriTavern iOS 仅在展开编辑器实际放不下时收窄内容区并让字符数与全屏按钮换行，避免正文、字符数或全屏按钮被右侧裁掉。
- 安卓打开布局调节框后，纵向滑动会继续滚动，只有明确的横向手势或轻点轨道才会修改滑杆。
- 安卓自定义“普通条目高度”时会关闭条目自身的离屏尺寸缓存，避免缩小条目后互相重叠；恢复默认高度后继续使用原有性能优化。
- “预设名称框长度”与“分支名称框长度”使用固定且独立的调节范围；切换布局或调节其中一项不会再锁死、夹断或放大另一项。
- 条目间距小于 `3px` 时，拖拽落点线自动切换到目标卡片内侧；正常间距继续使用原版外部主题色细线。
- 手机对比预设时，点击“内容不同”或“正文相似”会自动展开两侧目标分组并读取正文，不再要求先切换为平铺显示。
- 对比模式下，差异条目保留红色、黄色或蓝色边框，同时叠加原版随主题变化的上方／下方细落点线。
- TauriTavern iOS 会为“对比结果详情”和“预设正则脚本·搬运”自动避开灵动岛与底部安全区；TauriTavern Windows 会改用仅限工坊内部的指针拖拽，避开原生文件拖放的禁止标志。

## 目录

- `manifest.json`：SillyTavern 扩展清单。
- `dist/index.js`：标准 SillyTavern 扩展启动器。
- `dist/workshop-v3.02.js`：当前业务入口；包含最终“开关快照”、手机标题栏与可视区域定位，以及电脑主面板／分屏适配。
- `dist/workshop-v3.01.js`：上一版业务入口，保留用于版本回溯。
- `dist/preset-content-editor.js`：独立的预设正文全屏编辑器，不包含世界书功能。
- `dist/workshop-v2.53.js`：由 v2.53 JSON 的 `content` 原样提取的迁移基准。
- `bridge/`：复用酒馆助手运行环境所需的兼容桥。
- `legacy/`：迁移前的原始酒馆助手脚本，仅用于校验和回退。
- `scripts/validate.mjs`：发布前的基础完整性检查。

## 安全说明

第三方扩展能够在 SillyTavern 页面中运行代码。请只从本项目的正式仓库安装，并在更新前查看版本说明。
